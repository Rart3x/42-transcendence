version: '3'
services:
  app:
    build: .
    container_name: vue-app
    ports:
      - "8080:8080"
  
  nginx:
    image: nginx:alpine
    container_name: nginx-app
    volumes: 
     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:  
      - 80:80
    depends_on:
      - app 